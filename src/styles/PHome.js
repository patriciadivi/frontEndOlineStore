import styled from 'styled-components';

// eslint-disable-next-line import/prefer-default-export
export const PHome = styled.div`
  p {
    text-align: center;
    color: red;
    font-size: 1.3rem;
  }
`;
